import * as React from 'react';

interface IManageMembersProps {
}

const ManageMembers: React.FunctionComponent<IManageMembersProps> = (props) => {
  return (
    <>ManageMembers</>
  );
};

export default ManageMembers;
